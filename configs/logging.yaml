version: 1

formatters:
  standard:
    format: "%(asctime)s | %(levelname)s | %(message)s"

handlers:
  file:
    class: logging.FileHandler
    formatter: standard
    filename: logs/pipeline.log

root:
  level: INFO
  handlers: [file]